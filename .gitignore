# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Database
*.db
*.sqlite
*.sqlite3

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Flask
instance/
.webassets-cache

# Environment variables
.env
.env.local

# Dev password-reset fallback (contains sensitive links)
password_reset_last_link.txt

# Pytest
.pytest_cache/

# Nested duplicate project tree (legacy copy with its own .git). Kept on disk
# for anyone who needs to recover uncommitted drafts, but ignored by this repo
# so `git status` does not surface it as untracked. Review and delete manually
# once the duplicate is confirmed obsolete.
/IntelligentNameplateFinal/

